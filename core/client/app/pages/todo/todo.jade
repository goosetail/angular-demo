#todo

	.back.fa.fa-arrow-left(ui-sref="index")

	h2 {{list.name}}

	ul.list
		li.component(todo-item, ng-repeat = "item in list.items", item = "item")

	.add-item
		label Enter new item:&nbsp;
		input(ng-model="newItem.task")
		button.btn-primary(ng-click="addItem()") Add Item